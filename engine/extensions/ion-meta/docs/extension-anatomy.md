# Extension Anatomy (pointer)

This in-tree doc was retired to prevent drift against the canonical reference.

Read the canonical source instead:

- **Inside an Ion session**: call `ion_read_doc path: extensions/anatomy.md` from the ion-meta orchestrator.
- **In the repo**: `docs/extensions/anatomy.md` is the source of truth maintained alongside the engine code.
- **At an installed ion-meta** (when this file lives under `~/.ion/extensions/ion-meta/docs/`): the canonical copy is bundled at `~/.ion/extensions/ion-meta/docs/canonical/extensions/anatomy.md`, installed there by `engine/commands/install.command`.

The historical contents of this file -- a hand-written duplicate of the canonical doc -- drifted three different ways before this collapse. Treat ion-meta's `docs/canonical/` tree as the authoritative copy.
